# Frontend Module

This module is a React app powered by Vite.

## Run directly

```bash
npm install
npm run dev
```

## Run through Gradle

From the repository root:

```bash
./gradlew :frontend:run
```

If you do not have Gradle Wrapper yet, run with local Gradle:

```bash
gradle :frontend:run
```
